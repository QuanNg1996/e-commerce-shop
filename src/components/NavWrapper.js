import styled from 'styled-components';

// styled components for the NavBar
export const NavWrapper = styled.nav `
background: var(--mainDark);
.nav-link {
  text-color: var(--mainWhite)!important;
  font-size: 1.3rem;
  /* 1rem = 20px */
  text-transform: capitalize;
  font-weight: bold;
}
`
